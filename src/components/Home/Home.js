import React from 'react';

import Aux from '../../hoc/Aux/Aux';
import './Home.css';

const home = ( props ) => (
    <Aux>
        <h1>List</h1>
        <hr></hr>
    </Aux>
);

export default home;