import React from 'react';
import Aux from '../../hoc/Aux/Aux';

const noActivation = ( props ) => (
    <Aux>
        <h1>Activate account</h1>
        <hr></hr>
        <p>Invalid url</p>
    </Aux>
);

export default noActivation;